# recurrent / LSTM 短训验证结果模板

训练批次：`2026-04-23-10`  
实验目的：`recurrent/LSTM 运行时确证`  
日期：`2026-04-23`  
代码版本：`main@789c4afc560653c85290485f1e642beb274000a3`  
模型版本说明：`gorge_chase_discrete_sac_recurrent`，运行时明确包含 `LSTM(hidden=64, layers=1)`，并启用 `seq_len=12 / burn_in=4 / learn_len=8`。  
模型ID / ckpt：`gorge_chase_diy_0 / model.ckpt-3000.pkl`  
是否从旧 checkpoint 继续训练：`否`  
运行模式：`train`  
备注：本批次日志时间窗口为 `2026-04-23 10:34:03.430794 ~ 2026-04-23 11:20:58.676317`。当前已经能从日志确认“recurrent 模型 + sequence learner”两件事，但仍缺 `[RNN-SAMPLE]` 与 `[RNN-STATE]`，所以还不能确认在线推理阶段真的维护了 hidden state。

---

## 1. 训练配置
map = `10`
map_random = `1`
treasure_count = `10`
buff_count = `2`
buff_cooldown = `200`
talent_cooldown = `150`
monster_interval = `200`
monster_speedup = `N/A（日志只暴露 monster_speed，最新 training_metrics 为 1.88）`
max_step = `1000`

训练总步数 = `3004`
episode 数 = `7594`

---

## 2. 本轮最重要的结论（先填）
1. 这次训练是否能确认“真的启用了 recurrent/LSTM”：`是`
2. 这次训练是否能确认“learner 真的按 sequence 在训练”：`是`
3. 这次训练是否能确认“agent 在线推理真的维护了 hidden state”：`不能确认`
4. 当前最主要的问题是：`缺少 [RNN-SAMPLE] 和 [RNN-STATE] 日志，collector 与在线 hidden state 这两段仍没有直接证据`
5. 是否值得进入下一轮 2万~3万 step 短训：`否`

---

## 3. [RNN-CONFIG] 运行时真值（必须）
请直接从日志提取，不要推断；没有就写 `N/A`

use_recurrent = `true`
seq_len = `12`
burn_in = `4`
learn_len = `8`
lstm_hidden_dim = `64`
lstm_num_layers = `1`
use_gru = `false`
static_hidden_dim = `64`
dynamic_hidden_dim = `64`

请贴 1~3 条原始日志：
```text
{"time": "2026-04-23 10:34:39.442836", "level": "INFO", "message": "aisrv [RNN-CONFIG] {\"burn_in\": 4, \"dynamic_hidden_dim\": 64, \"learn_len\": 8, \"lstm_hidden_dim\": 64, \"lstm_num_layers\": 1, \"seq_len\": 12, \"static_hidden_dim\": 64, \"use_gru\": false, \"use_recurrent\": true}", "file": "algorithm.py", "line": "83", "module": "aisrv", "process": "algorithm", "function": "__init__", "stack": "", "pid": 289}
{"time": "2026-04-23 10:34:39.461571", "level": "INFO", "message": "aisrv [RNN-CONFIG] {\"burn_in\": 4, \"dynamic_hidden_dim\": 64, \"learn_len\": 8, \"lstm_hidden_dim\": 64, \"lstm_num_layers\": 1, \"seq_len\": 12, \"static_hidden_dim\": 64, \"use_gru\": false, \"use_recurrent\": true}", "file": "train_workflow.py", "line": "193", "module": "aisrv", "process": "train_workflow", "function": "workflow", "stack": "", "pid": 289}
```

补一句判断：
- 当前运行时配置是否明确显示启用了 recurrent：`是`

---

## 4. [RNN-MODEL] 模型结构真值（必须）
请直接从日志提取，不要推断；没有就写 `N/A`

model_name = `gorge_chase_discrete_sac_recurrent`
use_recurrent = `true`
has_lstm_module = `true`
has_gru_module = `false`
recurrent_module_name = `lstm`
recurrent_hidden_dim = `64`
actor_param_count = `85328`
critic_param_count = `170656`
total_param_count = `255984`
has_key_lstm_weight_ih_l0 = `true`
has_key_lstm_weight_hh_l0 = `true`

请贴 1~3 条原始日志：
```text
{"time": "2026-04-23 10:34:39.450155", "level": "INFO", "message": "aisrv [RNN-MODEL] {\"actor_param_count\": 85328, \"critic_param_count\": 170656, \"has_gru_module\": false, \"has_key_recurrent_weight_hh_l0\": true, \"has_key_recurrent_weight_ih_l0\": true, \"has_lstm_module\": true, \"model_name\": \"gorge_chase_discrete_sac_recurrent\", \"recurrent_hidden_dim\": 64, \"recurrent_key_sample\": [\"actor.recurrent.weight_ih_l0\", \"actor.recurrent.weight_hh_l0\", \"actor.recurrent.bias_ih_l0\", \"actor.recurrent.bias_hh_l0\", \"q1.recurrent.weight_ih_l0\"], \"recurrent_module_name\": \"lstm\", \"total_param_count\": 255984, \"use_recurrent\": true}", "file": "agent.py", "line": "48", "module": "aisrv", "process": "agent", "function": "__init__", "stack": "", "pid": 289}
```

补一句判断：
- 当前初始化模型是否已经明确包含 recurrent 参数：`是`

---

## 5. [RNN-CKPT] checkpoint 加载真值（必须）
请直接从日志提取，不要推断；没有就写 `N/A`

checkpoint_path = `/data/ckpt/gorge_chase_diy/models/model.ckpt_gorge_chase_diy_0/model.ckpt-2900.pkl`
load_success = `true`
model_expects_recurrent = `true`
checkpoint_has_recurrent_keys = `true`
missing_key_count = `0`
unexpected_key_count = `0`
shape_mismatch_count = `0`
strict_load = `true`
recurrent_key_sample = `["actor.recurrent.weight_ih_l0", "actor.recurrent.weight_hh_l0", "actor.recurrent.bias_ih_l0", "actor.recurrent.bias_hh_l0", "q1.recurrent.weight_ih_l0"]`

请贴 1~3 条原始日志：
```text
{"time": "2026-04-23 10:35:41.057705", "level": "INFO", "message": "aisrv [RNN-CKPT] {\"checkpoint_has_recurrent_keys\": true, \"checkpoint_path\": \"/data/ckpt/gorge_chase_diy/models_new//model.ckpt_gorge_chase_diy_0/model.ckpt-0.pkl\", \"load_success\": true, \"missing_key_count\": 0, \"model_expects_recurrent\": true, \"recurrent_key_sample\": [\"actor.recurrent.weight_ih_l0\", \"actor.recurrent.weight_hh_l0\", \"actor.recurrent.bias_ih_l0\", \"actor.recurrent.bias_hh_l0\", \"q1.recurrent.weight_ih_l0\"], \"shape_mismatch_count\": 0, \"strict_load\": true, \"unexpected_key_count\": 0}", "file": "algorithm.py", "line": "434", "module": "aisrv", "process": "algorithm", "function": "load_checkpoint", "stack": "", "pid": 289}
{"time": "2026-04-23 10:40:55.243666", "level": "INFO", "message": "aisrv [RNN-CKPT] {\"checkpoint_has_recurrent_keys\": true, \"checkpoint_path\": \"/data/ckpt/gorge_chase_diy/models/model.ckpt_gorge_chase_diy_0/model.ckpt-200.pkl\", \"load_success\": true, \"missing_key_count\": 0, \"model_expects_recurrent\": true, \"recurrent_key_sample\": [\"actor.recurrent.weight_ih_l0\", \"actor.recurrent.weight_hh_l0\", \"actor.recurrent.bias_ih_l0\", \"actor.recurrent.bias_hh_l0\", \"q1.recurrent.weight_ih_l0\"], \"shape_mismatch_count\": 0, \"strict_load\": true, \"unexpected_key_count\": 0}", "file": "algorithm.py", "line": "434", "module": "aisrv", "process": "algorithm", "function": "load_checkpoint", "stack": "", "pid": 289}
{"time": "2026-04-23 11:20:56.359111", "level": "INFO", "message": "aisrv [RNN-CKPT] {\"checkpoint_has_recurrent_keys\": true, \"checkpoint_path\": \"/data/ckpt/gorge_chase_diy/models/model.ckpt_gorge_chase_diy_0/model.ckpt-2900.pkl\", \"load_success\": true, \"missing_key_count\": 0, \"model_expects_recurrent\": true, \"recurrent_key_sample\": [\"actor.recurrent.weight_ih_l0\", \"actor.recurrent.weight_hh_l0\", \"actor.recurrent.bias_ih_l0\", \"actor.recurrent.bias_hh_l0\", \"q1.recurrent.weight_ih_l0\"], \"shape_mismatch_count\": 0, \"strict_load\": true, \"unexpected_key_count\": 0}", "file": "algorithm.py", "line": "434", "module": "aisrv", "process": "algorithm", "function": "load_checkpoint", "stack": "", "pid": 289}
```

补一句判断：
- 当前 checkpoint 与 recurrent 模型是否匹配：`是`
- 是否存在“代码是 recurrent，但 ckpt 不是 recurrent”的迹象：`否`

---

## 6. [RNN-SAMPLE] sequence sample 真值（必须）
请直接从日志提取，不要推断；没有就写 `N/A`

use_recurrent = `N/A`
collector_len = `N/A`
sequence_sample_count = `N/A`
seq_len = `N/A`
burn_in = `N/A`
learn_len = `N/A`
padding_used = `N/A`
mask_used = `N/A`
first_sequence_shape = `N/A`
effective_timestep_count = `N/A`
avg_effective_timestep_count = `N/A`

请贴 1~5 条原始日志：
```text
N/A（当前批次未看到 [RNN-SAMPLE]）
```

补一句判断：
- 当前 sample_process 是否真的切出了 sequence sample：`不能直接确认`
- `sequence_sample_count` 是否明确 > 0：`不能直接确认`

---

## 7. [RNN-LEARN] learner 序列训练真值（必须）
请直接从日志提取，不要推断；没有就写 `N/A`

use_recurrent = `true`
batch_size = `2048`
has_time_dim = `true`
obs_shape = `[2048, 12, 82]`
legal_action_shape = `[2048, 12, 16]`
act_shape = `[2048, 12, 1]`
reward_shape = `[2048, 12, 1]`
mask_shape = `[2048, 12, 1]`
done_shape = `[2048, 12, 1]`
effective_token_count = `16191 ~ 16254`
burn_in = `4`
learn_len = `8`

请贴 1~5 条原始日志：
```text
{"time": "2026-04-23 10:35:48.678413", "level": "INFO", "message": "learner [RNN-LEARN] {\"act_shape\": [2048, 12, 1], \"batch_size\": 2048, \"burn_in\": 4, \"done_shape\": [2048, 12, 1], \"effective_token_count\": 16191, \"has_time_dim\": true, \"learn_len\": 8, \"legal_action_shape\": [2048, 12, 16], \"mask_shape\": [2048, 12, 1], \"obs_shape\": [2048, 12, 82], \"reward_shape\": [2048, 12, 1], \"temporal_obs_shape\": [2048, 12, 44], \"use_recurrent\": true}", "file": "algorithm.py", "line": "138", "module": "learner", "process": "algorithm", "function": "learn", "stack": "", "pid": 196}
{"time": "2026-04-23 10:38:45.434985", "level": "INFO", "message": "learner [RNN-LEARN] {\"act_shape\": [2048, 12, 1], \"batch_size\": 2048, \"burn_in\": 4, \"done_shape\": [2048, 12, 1], \"effective_token_count\": 16213, \"has_time_dim\": true, \"learn_len\": 8, \"legal_action_shape\": [2048, 12, 16], \"mask_shape\": [2048, 12, 1], \"obs_shape\": [2048, 12, 82], \"reward_shape\": [2048, 12, 1], \"temporal_obs_shape\": [2048, 12, 44], \"use_recurrent\": true}", "file": "algorithm.py", "line": "138", "module": "learner", "process": "algorithm", "function": "learn", "stack": "", "pid": 196}
{"time": "2026-04-23 10:41:40.430817", "level": "INFO", "message": "learner [RNN-LEARN] {\"act_shape\": [2048, 12, 1], \"batch_size\": 2048, \"burn_in\": 4, \"done_shape\": [2048, 12, 1], \"effective_token_count\": 16212, \"has_time_dim\": true, \"learn_len\": 8, \"legal_action_shape\": [2048, 12, 16], \"mask_shape\": [2048, 12, 1], \"obs_shape\": [2048, 12, 82], \"reward_shape\": [2048, 12, 1], \"temporal_obs_shape\": [2048, 12, 44], \"use_recurrent\": true}", "file": "algorithm.py", "line": "138", "module": "learner", "process": "algorithm", "function": "learn", "stack": "", "pid": 196}
{"time": "2026-04-23 10:44:39.267544", "level": "INFO", "message": "learner [RNN-LEARN] {\"act_shape\": [2048, 12, 1], \"batch_size\": 2048, \"burn_in\": 4, \"done_shape\": [2048, 12, 1], \"effective_token_count\": 16235, \"has_time_dim\": true, \"learn_len\": 8, \"legal_action_shape\": [2048, 12, 16], \"mask_shape\": [2048, 12, 1], \"obs_shape\": [2048, 12, 82], \"reward_shape\": [2048, 12, 1], \"temporal_obs_shape\": [2048, 12, 44], \"use_recurrent\": true}", "file": "algorithm.py", "line": "138", "module": "learner", "process": "algorithm", "function": "learn", "stack": "", "pid": 196}
{"time": "2026-04-23 10:47:39.599331", "level": "INFO", "message": "learner [RNN-LEARN] {\"act_shape\": [2048, 12, 1], \"batch_size\": 2048, \"burn_in\": 4, \"done_shape\": [2048, 12, 1], \"effective_token_count\": 16254, \"has_time_dim\": true, \"learn_len\": 8, \"legal_action_shape\": [2048, 12, 16], \"mask_shape\": [2048, 12, 1], \"obs_shape\": [2048, 12, 82], \"reward_shape\": [2048, 12, 1], \"temporal_obs_shape\": [2048, 12, 44], \"use_recurrent\": true}", "file": "algorithm.py", "line": "138", "module": "learner", "process": "algorithm", "function": "learn", "stack": "", "pid": 196}
```

补一句判断：
- 当前 learner 是否明确吃到了 `[B, T, ...]` 的 batch：`是`
- 是否能明确证明不是单步 batch 伪装：`是`

---

## 8. [RNN-STATE] hidden state 在线维护真值（必须）
请直接从日志提取，不要推断；没有就写 `N/A`

### 8.1 reset 日志
hidden_reset = `N/A`
hidden_shape = `N/A`
cell_shape = `N/A`

请贴 1~3 条原始日志：
```text
N/A（当前批次未看到 [RNN-STATE] {"event":"reset", ...}）
```

### 8.2 step 日志
请至少提取 3~5 条，优先包含：
- 前几步 step
- hidden_in_norm / hidden_out_norm
- action
- guidance_source（如果有）
- flash_eval_trigger（如果有）

```text
N/A（当前批次未看到 [RNN-STATE] {"event":"step", ...}）
```

补一句判断：
- hidden state 是否真的在 reset 时清零：`不能确认`
- hidden state 是否真的随 step 更新，而不是常量全零：`不能确认`

---

## 9. 训练稳定性
ERROR = `0`
WARNING = `0`
Traceback = `0`
Exception = `0`
nan loss = `0`
error_cnt_sum = `0`

latest total_loss = `-0.1928`
avg total_loss = `0.1325`

latest critic_loss = `0.2434`
avg critic_loss = `0.3106`

latest policy_loss = `-0.4362`
avg policy_loss = `-0.1781`

latest alpha = `0.0500`
avg alpha = `0.0500`

latest entropy = `0.9921`
avg entropy = `1.0444`

是否出现 shape mismatch：`否`
是否出现 hidden state 维度错误：`否`
是否出现 mask 维度错误：`否`
是否出现 target Q 维度错误：`否`

如果出现错误，请贴 1~5 条原始报错：
```text
N/A
```

---

## 10. 短训基础表现（次要）
这部分不是本轮主目标，但可顺手记录。

| 指标 | 数值 |
| --- | ---: |
| avg_score / sim_score | 876.8708 |
| avg_steps | 389.4494 |
| avg_treasures | 2.9270 |
| avg_reward | 4.4968 |
| flash_count_sum | 500 |

注：以上基础表现按当前批次 `aisrv_kaiwu_rl_helper_pid289` 中去重后的 `[GAMEOVER]` 汇总，`flash_count_sum` 按日志里的 `flash:` 字段求和。

---

## 11. Flash / Planner 是否被破坏（次要但建议）
如果有这些字段就填，没有写 `N/A`

danger_flash_rate = `0.9981`
safe_flash_rate = `0.0019`
effective_flash_rate = `0.9911`
flash_leave_danger_rate = `0.9822`
wall_cross_effective_rate = `0.7978`
flash_eval_trigger_rate = `0.0087`
best_flash_better_than_move_rate = `0.9830`

补一句判断：
- 接入 recurrent 后，flash planner 是否明显失效：`否，当前日志里没有看到明显失效迹象`
- 当前版本是否仍保持 danger 主导 flash：`是`

注：本节 rate 为 `[GAMEOVER]` 中对应 rate 字段的 episode 级平均值。

---

## 12. 硬判定（必须逐条写）
1. 是否看到 `[RNN-CONFIG]`：`是`
2. 是否看到 `[RNN-MODEL]`：`是`
3. 是否看到 `[RNN-CKPT]`：`是`
4. 是否看到 `[RNN-SAMPLE]`：`否`
5. 是否看到 `[RNN-LEARN]`：`是`
6. 是否看到 `[RNN-STATE] reset`：`否`
7. 是否看到 `[RNN-STATE] step`：`否`
8. 是否能明确证明 `use_recurrent = true`：`是`
9. 是否能明确证明 `sequence_sample_count > 0`：`否`
10. 是否能明确证明 `has_time_dim = true`：`是`
11. 是否能明确证明 hidden state 在 step 间更新：`否`

---

## 13. 最终结论（必须）
请直接按下面格式写：

### 13.1 关于 recurrent 真值
- 这轮是否能确认“真的启用了 recurrent/LSTM”：`是`
- 证据是：`[RNN-CONFIG]` 里 `use_recurrent=true`，`[RNN-MODEL]` 里 `has_lstm_module=true`，`[RNN-CKPT]` 里 checkpoint 明确带 recurrent keys。
- 还缺什么证据：`就“是否启用 recurrent”这件事本身，不缺关键证据。`

### 13.2 关于 sequence learner
- 这轮是否能确认“真的按序列训练”：`是`
- 证据是：`[RNN-LEARN]` 明确给出 `has_time_dim=true`，并且 `obs / action / reward / mask / done` 全部是 `[2048, 12, ...]`。
- 还缺什么证据：`仍缺 [RNN-SAMPLE]，所以 collector 侧“如何切序列”这一步没有直接日志。`

### 13.3 关于在线 hidden state
- 这轮是否能确认“真的在线维护 hidden state”：`不能确认`
- 证据是：`当前没有 [RNN-STATE] reset / step 日志，无法直接看到 reset 清零、step 更新、hidden norm 变化。`
- 还缺什么证据：`至少需要 [RNN-STATE] 的 reset 与 step 样例，最好再补 hidden_in_norm / hidden_out_norm。`

### 13.4 是否进入下一轮
- 是否建议继续跑 2万~3万 step 短训：`否`
- 下一轮之前必须先修的点是：`先补 [RNN-SAMPLE] 与 [RNN-STATE] 日志，把 collector 与在线 hidden state 这两段跑通并证实，再进入更长短训。`

---

## 14. 你的附加说明（可选）
- `[RNN-CKPT]` 日志在一轮中重复很多次，但内容口径一致，说明 actor 侧持续加载到的是 recurrent checkpoint。
- `[RNN-SAMPLE]` 与 `[RNN-STATE]` 当前完全缺失，所以这轮结论里“在线推理维护 hidden state”只能停留在不能确认，不能强行外推。
- 当前最怀疑的运行时问题不是 learner，而是 collector / online inference 这两段虽然很可能已经接了 recurrent，但日志证据链还没闭合。
